networks:
  finance-network:
    name: "finance-network"
services:
  app:
      container_name: api_finance
      build:
        dockerfile: Dockerfile
        network: host
        context: .
      environment:
        DATABASE_URL: "jdbc:postgresql://postgres.lpaoqhkjphpnfajyxogk:password@aws-0-sa-east-1.pooler.supabase.com:6543/postgres"
      ports:
        - "8080:8080"
      # links:
      #   - db
      networks:
        - finance-network
      env_file:
        - .env

  # db:
  #   image: postgres:15
  #   container_name: db_finance
  #   environment:
  #     POSTGRES_USER: user
  #     POSTGRES_PASSWORD: password
  #     POSTGRES_DB: mydb
  #   ports:
  #     - "5555:5432"
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #     - ./db/init:/docker-entrypoint-initdb.d
  #   networks:
  #     - finance-network
  # healthcheck:
  #   test: ["CMD-SHELL", "pg_isready -U user -d mydb"]
  #   interval: 5s
  #   timeout: 5s
  #   retries: 5


volumes:
  pgdata:
